name: Deploy docker images to GCP Artifact registry
on:
    push:
      branches: ["main"]
    pull_request:
      branches: ["main"]  

jobs:
    deploygcp:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4
            - name: Authenticate to Google Cloud
              uses: google-github-actions/auth@v1
              with:
                workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
                service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}
                contents: read
                pages: write
                id-token: write

#             - name: Configure Docker for Google Cloud
#               run: gcloud auth configure-docker
#             - name: Build and Push Docker Image to GCP Artifact Registry
#               run: |
#                 docker build -t us-central1-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.GAR_REPOSITORY }}/${{ env.IMAGE_NAME }}:latest .
#                 docker push us-central1-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.GAR_REPOSITORY }}/${{ env.IMAGE_NAME }}:latest

#             - name: Login to Artifact Registry
#               uses: docker/login-action@v3
#               with:
#                 registry: "${{ env.GCP_REGION }}-docker.pkg.dev"
#                 username: "oauth2accesstoken"
#                 password: "${{ steps.auth.outputs.access_token }}"

#             - id: 'gcloud'
#               name: 'gcloud'
#               run: |-
#                 gcloud auth login --brief --cred-file="${{ steps.auth.outputs.credentials_file_path }}"
#                 gcloud services list

# # extracting metadata
#             - name: Extract Docker metadata
#               id: meta
#               uses: docker/metadata-action@v5
#               with:
#                 images: ${{ env.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.GAR_REPOSITORY }}/${{ env.IMAGE_NAME }}

                
#             - name: Build and push Docker image
#               uses: docker/build-push-action@v6
#               with:
#                 context: .
#                 push: ${{ github.event_name != 'pull_request' }}
#                 tags: ${{ steps.meta.outputs.tags }}
#                 labels: ${{ steps.meta.outputs.labels }}
#                 cache-from: type=gha
#                 cache-to: type=gha,mode=max


            
      