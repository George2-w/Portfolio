apiVersion: v1
kind: Service
metadata:
  name: nginx-sailorservice
  labels:
    app: my-app
spec:
  type: LoadBalancer
  selector:
    app: my-app
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
